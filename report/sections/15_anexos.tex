\section{Anexos}\label{sec:anexos}

\subsection{A1. Fuente y diccionario}
La base ONSV conserva encabezados descriptivos en la hoja \texttt{SINIESTROS}. El mapeo a nombres canónicos está en \path{src/block_b_dataset_audit.py}; la auditoría agregada está en \path{report/tables/tab00_audit_summary_block_b.json}.

\subsection{A2. Artefactos definitivos}
\begin{itemize}
  \item Bundle de inferencia y manifiesto: \path{models/final/}.
  \item Protocolo: \path{report/tables/final_model_protocol.md}.
  \item Métricas, probabilidades, confusión, errores e intervalos: tablas de referencia final.
  \item Selección y robustez: tablas de selección y \path{models/final/model_selection.json}.
  \item Explicabilidad: tablas y figura de importancia global.
\end{itemize}

\subsection{A3. Configuración congelada}
\begin{lstlisting}
canonical = {
    "config_id": "MLP_64_32",
    "hidden_units": [64, 32],
    "dropout": 0.35,
    "l2": 3e-4,
    "learning_rate": 5e-4,
    "batch_size": 64,
    "seed": 314,
    "raw_threshold": 0.65,
    "calibrated_threshold": 0.20,
    "calibration": "platt",
}
\end{lstlisting}

\subsection{A4. Protocolo cronológico}
\begin{lstlisting}
train = data[data.FECHA.dt.year.isin([2021, 2022])]
selection = data[data.FECHA.dt.year.eq(2023)]
reference = data[data.FECHA.dt.year.isin([2024, 2025])]

# Fit preprocessing and weights: train only.
# Select architecture, seed, calibration and thresholds: selection only.
# Reference 2024-2025: reporting only; no further tuning.
\end{lstlisting}

\subsection{A5. Decisiones metodológicas}
\small
\begin{longtable}{p{2.2cm}p{5.6cm}p{3cm}p{3.2cm}}
\caption{Controles del modelo definitivo.}\label{tab:bitacora}\\
\toprule
Etapa & Decisión & Riesgo controlado & Evidencia \\
\midrule
\endfirsthead
\toprule
Etapa & Decisión & Riesgo controlado & Evidencia \\
\midrule
\endhead
Protocolo & Separar 2021--22, 2023 y 2024--25 por rol & Fuga y optimismo por mezcla cronológica & Protocolo final \\
Selección & Comparar 3 arquitecturas $\times$ 3 semillas; congelar \texttt{MLP\_64\_32}, semilla 314 & Selección oportunista & Registro de selección \\
Calibración & Elegir Platt mediante Brier OOF de 5 folds en 2023 & Ajustar y evaluar calibrador en los mismos casos & Registro de calibración \\
Referencia & Tratar 2024--25 como periodo ya observado & Reutilizarlo para nuevos ajustes & Métricas finales \\
SHAP & Gradient SHAP solo con entrenamiento y selección & Interpretación contaminada por la referencia & Procedencia SHAP \\
Interfaz & Bundle verificado, solo lectura, decisión en escala calibrada & Divergencia informe/aplicación & Manifiesto canónico \\
\bottomrule
\end{longtable}

\subsection{A6. Reproducción y verificación}
\begin{lstlisting}[language=bash]
# Rebuild evidence without retraining the MLP
.venv/bin/python src/final_model_bundle.py
.venv/bin/python src/final_evaluation_figures.py
.venv/bin/python src/final_explainability.py

# Contracts and GUI
.venv/bin/python tests/test_model_protocol.py
.venv/bin/python tests/test_final_model_bundle.py
.venv/bin/python tests/test_final_explainability.py
.venv/bin/python tests/test_app_inference.py
.venv/bin/python src/block_g_app_check.py

# Interface
streamlit run app/streamlit_app.py

# Report
cd report && latexmk -pdf -interaction=nonstopmode main.tex
\end{lstlisting}

Las versiones exactas usadas para el bundle constan en el manifiesto. La reproducción no autoriza reoptimizar el modelo con la referencia 2024--2025.
